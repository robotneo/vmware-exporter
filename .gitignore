# Environments
.DS_Store
local/
.env
.venv
env/
venv/
old/
test/
ENV/
env.bak/
venv.bak/
vendor/
dist/

# Agent workspace (not a build cache, but session-local)
.workbuddy/

# Backups written by scripts/patch_dashboards.py so a patch can be reverted
*.json.bak
